name: Build Image

on: 
  push:
    branches:
      - dev
      - staging
  workflow_dispatch:

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Log Mantap"
